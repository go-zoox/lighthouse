package constants

var (
	// Version is the current version of lighthouse
	Version = "unknown"
	// CommitHash is the current commit hash of lighthouse
	CommitHash = "unknown"
	// BuildTime is the time when lighthouse was built
	BuildTime = "unknown"
)
